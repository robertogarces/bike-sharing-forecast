# Azure ML Environment for the training job — built from conda.yaml on an Ubuntu 22.04 base image.
name: bike-sharing-env
version: "7"
conda_file: conda.yaml
image: mcr.microsoft.com/azureml/openmpi4.1.0-ubuntu22.04
